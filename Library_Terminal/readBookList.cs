using System;
using System.IO;
using System.Collections.Generic;
namespace Library_Terminal
{
	public class ReadBookList
	{

		List<string> BookList = new List<string>();

		StreamReader SR = new StreamReader("../../../librarytext.txt");

		foreach (Book book in ReadBookList)
		{
			Console.WriteLine(List<string> readBookList)

		}	

	}
}

